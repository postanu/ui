import type { Meta, StoryObj } from '@storybook/vue3'

import PIconThreads from './PIconThreads.vue'

type Story = StoryObj<typeof PIconThreads>

export default {
	title: 'Icons / PIconThreads',
	component: PIconThreads
} as Meta<typeof PIconThreads>

export const Default: Story = {}
